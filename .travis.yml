sudo: required

services:
  - docker

language: bash

addons:
  apt:
    # sources:
    #   - debian-sid
    packages:
      - shellcheck

install:
  - docker-compose build
  # - docker images
  # - docker ps -a
  - ls -lah
  # - shellcheck /root/.dot/bin/dot

script:
  - docker-compose run test

notifications:
  email: false
