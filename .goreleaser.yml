# https://goreleaser.com
builds:
- env:
    - CGO_ENABLED=0
  # main: main.go
  # "{{ .Binary }}-{{ .Os }}-{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}"
  # binary: dot
  goos: # https://golang.org/doc/install/source#environment
    - windows
    - darwin
    - linux
    # - freebsd
  goarch:
    - 386
    - amd64
  #   - arm
  #   - arm64
  # goarm:
  #   - 6
  #   - 7
archive:
  # Upload the binaries directly
  # name_template and files are ignored
  format: binary # tar.gz (default), zip
  # replacements:
  #   darwin: Darwin
  #   linux: Linux
  #   windows: Windows
  #   386: i386
  #   amd64: x86_64
  # files: []
  # hooks: {}
# sign:
#    artifacts: checksum
# checksum:
#   name_template: 'checksums.txt'
snapshot:
  # Default is "SNAPSHOT-{{.Commit}}"
  name_template: "{{ .Tag }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
git:
  short_hash: true
brew:
  # name: dot
  # github:
  #   owner: LEI
  #   name: homebrew-tap
  # download_strategy:
  # commit_author:
  #   name:
  #   email:
  # folder: Formula
  # caveats: ""
  homepage: "https://github.com/LEI/dot"
  # description: ""
  dependencies:
    - git
    # - bash
  # conflicts: []
  # plist: |
  # test: |
  # install (default: bin.install "program"°
