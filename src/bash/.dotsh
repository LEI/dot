#!/usr/bin/env bash

directory "$state" "$HOME/.bash.d"
line_in_file "$state" "$HOME/.bashrc" '[[ -n "$PS1" ]] && [[ -f ~/.bash_profile ]] && source ~/.bash_profile'

_main() {
  symlink --prefix "$prefix" --path f:.bash*
}

_main "$@"
# unset _main
