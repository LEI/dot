#!/usr/bin/env bash


_post_install() {
  if hash nvim 2>/dev/null \
    && [[ -d "$HOME/.vim" ]] \
    && [[ ! -d "$HOME/.config/nvim" ]]
  then
    mkdir -l $HOME/.config
    ln -s $HOME/.vim $HOME/.config/nvim
  fi
}
