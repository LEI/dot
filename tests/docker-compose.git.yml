version: "2"

services:
  test:
    image: lei/dot
    depends_on:
      - image
    entrypoint: /bin/bash
    command: |
      -l -c '
      run dot -a git=LEI/dot-git
      run test -f "$$HOME/.gitconfig"'
