version: "2"

services:
  test:
    image: lei/dot
    depends_on:
      - image
    entrypoint: /bin/bash
    command: |
      -l -c '
      run dot -a vim=LEI/dot-vim
      run vim +PlugInstall +qall
      run test -d "$$HOME/.vim/plugged"'
