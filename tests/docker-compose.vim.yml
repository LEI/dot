version: "2"

services:
  test:
    image: lei/dot
    depends_on:
      - image
    entrypoint: /bin/bash
    command: |
      -l -c 'run() { printf "%s\n" "TEST: $$*"; "$$@" || exit 1; }
      run dot -a termux=LEI/dot-vim
      run vim +PlugInstall +qall
      run test -d "$$HOME/.vim/plugged"'
